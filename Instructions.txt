cd "C:\Users\jvz16\Proyectos\travesia_reservas"
.\.venv\Scripts\Activate.ps1

& "C:\Users\jvz16\anaconda3\python.exe" -m venv .venv

Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass

pip install -r requirements.txt

Copy-Item .env.example .env



python manage.py migrate
python manage.py runserver

http://127.0.0.1:8000/
http://127.0.0.1:8000/admin/
http://127.0.0.1:8000/dashboard/



cd "C:\Users\jvz16\Proyectos\travesia_reservas"
& "C:\Users\jvz16\anaconda3\python.exe" -m venv .venv
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt
Copy-Item .env.example .env
python manage.py migrate
python manage.py createsuperuser
python manage.py seed_data
python manage.py runserver


git status
git add -A
git commit --allow-empty -m "Changes"
git push origin main


& "C:\Users\jvz16\anaconda3\python.exe" -m venv .venv
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt
Copy-Item .env.example .env
python manage.py makemigrations
python manage.py migrate
python manage.py createsuperuser
python manage.py seed_data
python manage.py runserver

"whitenoise.middleware.WhiteNoiseMiddleware",
STATICFILES_STORAGE = "whitenoise.storage.CompressedManifestStaticFilesStorage"


- Verificar que migraciones no borran todo
- Pasar rango de tiempo de visualizaciones a la visualizacion
- Que vista "por mesa" en "Ver ocupacion" sea la vista por defecto, en lugar de "por franja"
- Simplificar vista en Ajusted del Admin, y poder editar/agregar mesas y horarios, # tel y mejorar idioma.

- Filtro por defecto en "VER RESERVAS" es "HOY".
- No poder reservar con menos de 2hrs de anticipacion?
- Agregar boton rapido de "habilitar mesa" en "ver" dentro de "ver reservas" cuya function sea precisamente habilitar una mesa ocupada para que pueda ser reservada de inmediato si asi se quisiera posteriormente.
- Se pueden hacer reservas despues de las 7pm, 8pm???





Veo lo siguiente en shell de render:
">>> from django.conf import settings
>>> print(settings.DATABASES["default"])
{'ENGINE': 'django.db.backends.sqlite3', 'NAME': PosixPath('/opt/render/project/src/db.sqlite3'), 'ATOMIC_REQUESTS': False, 'AUTOCOMMIT': True, 'CONN_MAX_AGE': 0, 'CONN_HEALTH_CHECKS': False, 'OPTIONS': {}, 'TIME_ZONE': None, 'USER': '', 'PASSWORD': '', 'HOST': '', 'PORT': '', 'TEST': {'CHARSET': None, 'COLLATION': None, 'MIGRATE': True, 'MIRROR': None, 'NAME': None}}
>>> "

Lo siguiente es settings.py:
"DATABASES = {
    "default": {"ENGINE": "django.db.backends.sqlite3", "NAME": BASE_DIR / "db.sqlite3"}
}"

Lo siguiente en render.yaml:
"services:
  - type: web
    name: travesia-reservas
    runtime: python
    plan: starter
    buildCommand: "./build.sh"
    startCommand: "gunicorn travesia_reservas.wsgi:application"
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: "False"
      - key: ALLOWED_HOSTS
        value: "travesia-reservas.onrender.com"
      - key: DATABASE_URL
        fromDatabase:
          name: travesia-db
          property: connectionString

databases:
  - name: travesia-db
    plan: basic-256mb"

...Dime si esto sirve para diagnosticar o si necesitas mas detalles como el seed, etc.